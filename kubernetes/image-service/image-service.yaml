apiVersion: v1
kind: Service
metadata:
  name: image-service
  namespace: nosql
spec:
  selector:
    app: image-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP