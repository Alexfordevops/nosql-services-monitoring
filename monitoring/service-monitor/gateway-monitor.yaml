apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gateway-service-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: gateway-service
  namespaceSelector:
    matchNames:
      - nosql
  endpoints:
    - port: "8080"
      path: /metrics
      interval: 15s